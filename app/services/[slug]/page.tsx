import {notFound} from "next/navigation";import Link from "next/link";import {services,clinic} from "@/config/elements";import {PageIntro,CTA} from "@/components/elements/shell";export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(s=>s.slug===slug);return {title:s?.title,description:s?.description,alternates:{canonical:`/services/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=services.find(s=>s.slug===slug);if(!s)notFound();return <><PageIntro eyebrow={s.title.toUpperCase()} title={s.tagline} description={s.description}/><section className="section wrap grid2"><div><h2>Your care, explained.</h2><p>Every visit starts with a conversation. Tell us what you would like help with, and our team will guide you through the next steps.</p><ul className="check-list">{s.highlights.map(h=><li key={h}>{h}</li>)}</ul></div><aside className="card"><p className="eyebrow">LET’S TALK ABOUT YOUR SMILE</p><h3>Request a consultation</h3><p>Select a preferred visit time. Our team will contact you to confirm availability.</p><Link className="btn" href={`/book-appointment?service=${s.slug}`} data-track-event="booking_click">Request a visit ↗</Link><p style={{marginTop:20}}><a href={`tel:${clinic.phone}`} data-track-event="call_click">{clinic.displayPhone}</a></p></aside></section><CTA/></>}
