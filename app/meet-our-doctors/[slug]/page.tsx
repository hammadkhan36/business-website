import Image from "next/image";import {notFound} from "next/navigation";import {doctors} from "@/config/elements";import {PageIntro,CTA} from "@/components/elements/shell";export function generateStaticParams(){return doctors.map(d=>({slug:d.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return {title:doctors.find(d=>d.slug===slug)?.name,alternates:{canonical:`/meet-our-doctors/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=doctors.find(d=>d.slug===slug);if(!d)notFound();return <><PageIntro eyebrow={d.role} title={d.name} description="Personal attention. Thoughtful dental care."/><section className="section wrap grid2"><div className="story-photo"><Image src={d.image} alt={d.name} fill sizes="(max-width:740px) 90vw,45vw"/></div><div><h2>Meet your dentist.</h2><p className="lead">{d.bio}</p><p>Your consultation is a chance to ask questions and discuss what matters to you.</p></div></section><CTA/></>}
