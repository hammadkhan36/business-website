import {notFound} from "next/navigation";import {PageIntro} from "@/components/elements/shell";import {RequestForm} from "@/components/elements/request-form";import {getElementsData} from "@/lib/website/elements-data";export const metadata={title:"Complete office form",robots:{index:false,follow:false}};export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const data=await getElementsData();const form=data?.forms.find(f=>f.slug===slug);if(!form)notFound();return <><PageIntro eyebrow="OFFICE FORM" title={form.name} description={form.description||"Send your response to our office."}/><section className="section wrap policy"><RequestForm kind="forms" form={form}/></section></>}
