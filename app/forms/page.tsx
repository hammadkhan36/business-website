import Link from "next/link";import {PageIntro} from "@/components/elements/shell";import {getElementsData} from "@/lib/website/elements-data";export const metadata={title:"Office forms",alternates:{canonical:"/forms"}};export default async function Page(){const data=await getElementsData();return <><PageIntro eyebrow="KEEPING YOUR VISIT SIMPLE" title="Office forms." description="Complete the form our team has asked you to fill out. For medical records or sensitive health information, please contact the office."/><section className="section wrap">{data?.forms.length?<div className="grid3">{data.forms.map(f=><Link className="card" key={f.id} href={`/forms/${f.slug}`}><h3>{f.name}</h3><p>{f.description}</p><span className="text-link">Open form ↗</span></Link>)}</div>:<p className="empty">No online office forms are currently available. Please call (714) 716-1155 for assistance.</p>}</section></>}
