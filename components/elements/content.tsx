import Link from "next/link";import Image from "next/image";import {services,doctors,faqs} from "@/config/elements";
export function ServiceCards({limit}:{limit?:number}){return <div className="grid3">{services.slice(0,limit).map((s,i)=><Link href={`/services/${s.slug}`} className="card" key={s.slug} data-track-event="service_click" data-track-label={s.title}><span className="card-number">{String(i+1).padStart(2,"0")}</span><h3>{s.title}</h3><p>{s.description}</p><span className="text-link">Explore this care ↗</span></Link>)}</div>}
export function DoctorCards(){return <div className="grid3">{doctors.map(d=><Link href={`/meet-our-doctors/${d.slug}`} className="doctor-card" key={d.slug}><div className="doctor-photo"><Image src={d.image} alt={d.name} fill sizes="(max-width:740px) 90vw,30vw"/></div><div className="doctor-info"><h3>{d.name} ↗</h3><p>{d.role}</p></div></Link>)}</div>}
export function FaqList({items=faqs}:{items?:{question:string;answer:string}[]}){return <div className="faq">{items.map(f=><details key={f.question}><summary>{f.question}</summary><p>{f.answer}</p></details>)}</div>}
